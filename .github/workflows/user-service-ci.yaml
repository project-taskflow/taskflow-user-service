name: User Service CI/CD

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      release_tag:
        description: Release tag in the format user-service-vX.Y.Z
        required: true
        type: string

jobs:
  ci:
    if: github.event_name != 'workflow_dispatch'
    uses: project-taskflow/taskflow/.github/workflows/reusable-ci.yaml@main
    with:
      trigger_event: ${{ github.event_name }}
      source_sha: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      service_name: user-service
      image_name: user-service
      helm_values_key: userService
      dev_manifest_path: infra/argocd/dev/user-service.yaml
      sonar_key: user-service
      docker_context: .
      dockerfile_path: ./Dockerfile
      working_directory: .
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_URL: ${{ secrets.SONAR_URL }}
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      TOKEN: ${{ secrets.TOKEN }}
      EMAIL_USERNAME: ${{ secrets.EMAIL_USERNAME }}
      EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}

  release:
    if: github.event_name == 'workflow_dispatch'
    uses: project-taskflow/taskflow/.github/workflows/reusable-ci.yaml@main
    with:
      release_tag: ${{ inputs.release_tag }}
      service_name: user-service
      image_name: user-service
      helm_values_key: userService
      dev_manifest_path: infra/argocd/dev/user-service.yaml
      prod_manifest_path: infra/argocd/prod/user-service.yaml
    secrets:
      TOKEN: ${{ secrets.TOKEN }}
      EMAIL_USERNAME: ${{ secrets.EMAIL_USERNAME }}
      EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}

